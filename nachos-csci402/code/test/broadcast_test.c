int main()
{
	Exec("../test/broadcast1",18);
	Exec("../test/broadcast2",18);
	/*Exec("../test/broadcast3",18);
	Exec("../test/broadcast4",18);*/
	Exec("../test/broadcast5",18);
	
	/*Some how we need to fix the fact we can only do 3 execs*/

	Exit(0);
}
